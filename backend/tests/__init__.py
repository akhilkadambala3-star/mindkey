"""Phase 2 unit tests for the deterministic evidence layer.

Run from the ``backend/`` directory:

    python -m unittest discover

These tests use in-memory fixtures only: no network, no Supabase, no
environment variables, and no LLM.
"""
